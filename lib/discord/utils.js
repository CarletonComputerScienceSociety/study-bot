const createChannelForMatch = (user1, user2) => {
  // TODO: implement this
  // should create a channel only visible to the two users
  // user1 and user2 are User objects, so you can use user1.discordId
};

module.exports.createChannelForMatch = createChannelForMatch;
