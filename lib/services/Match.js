class MatchService {
  async execute(discordId) {
    // TODO: implement this

    // find a user who matches with the current user (discordId)
    return { message: "Command not implemented yet." };
  }
}

module.exports.MatchService = MatchService;
